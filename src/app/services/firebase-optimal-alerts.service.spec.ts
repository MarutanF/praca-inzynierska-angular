import { TestBed } from '@angular/core/testing';

import { FirebaseOptimalAlertsService } from './firebase-optimal-alerts.service';

describe('FirebaseOptimalAlertsService', () => {
  // beforeEach(() => TestBed.configureTestingModule({}));

  // it('should be created', () => {
  //   const service: FirebaseOptimalAlertsService = TestBed.get(FirebaseOptimalAlertsService);
  //   expect(service).toBeTruthy();
  // });
});
